import { describe, it, expect } from "vitest";
import { render, screen } from "@testing-library/react";

describe("Modal Form Unit", () => {
	it("스켈레톤: 렌더링 테스트 자리", () => {
		expect(true).toBe(true);
	});
});
